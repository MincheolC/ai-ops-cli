{
  "name": "ai-ops-cli-workspace",
  "private": true,
  "workspaces": [
    "apps/cli",
    "apps/studio"
  ],
  "scripts": {
    "build": "npm run build --workspace=apps/cli",
    "test": "vitest run",
    "compile": "node apps/cli/dist/bin/index.js --help",
    "dev": "npm run dev --workspaces --if-present",
    "format": "prettier --write \"**/*.{ts,json,yaml,md}\"",
    "lint": "eslint .",
    "check": "npm run lint && npm run test",
    "studio:dev": "npm run build --workspace=apps/cli && AI_OPS_STUDIO_PROJECT_ROOT=\"${AI_OPS_STUDIO_PROJECT_ROOT:-$PWD}\" npm run tauri:dev --workspace=apps/studio",
    "studio:build": "npm run build --workspace=apps/cli && npm run build --workspace=apps/studio && npm run tauri:check --workspace=apps/studio",
    "studio:package:darwin-arm64": "npm run build --workspace=apps/cli && npm run tauri:release --workspace=apps/studio && node scripts/prepare-studio-platform-package.mjs",
    "studio:test": "npm run test --workspace=apps/studio",
    "publish:patch": "./scripts/publish.sh patch",
    "publish:minor": "./scripts/publish.sh minor",
    "publish:major": "./scripts/publish.sh major"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.56.1",
    "@typescript-eslint/parser": "^8.56.1",
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.1",
    "vitest": "^3.0.8"
  }
}
